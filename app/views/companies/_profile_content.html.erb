<div class="profile-content">
  <table class="outer" width="100%" id="contentTable" border="0" cellspacing="0" cellpadding="0">
    <tr>
      <td class="oRight" id="bodyCell">
        <div id="inner-content">
          <div id="profile-info" class="profile company">
            <ul class="nav nav-pills">
              <li class="active"><a data-toggle="tab" href="#company_profile">Profile</a></li>
              <li><a data-toggle="tab" href="#company_contacts">Yetkililer</a></li>
              <li><a data-toggle="tab" href="#company_docs">Taşımalar</a></li>
              <li><a data-toggle="tab" href="#company_docs">Açık İşlemler</a></li>
              <li><a data-toggle="tab" href="#company_docs">Aktiviteler</a></li>
              <li><a data-toggle="tab" href="#company_docs">Fırsatlar</a></li>
              <li><a data-toggle="tab" href="#company_docs">İlişkili Firmalar</a></li>
              <li><a data-toggle="tab" href="#company_docs">Notlar/Dosyalar</a></li>
              <li class="dropdown">
                <div class="btn-group">
                  <a class="dropdown-toggle btn btn-success" data-toggle="dropdown" href="#">
                    <i class="icon-cog icon-white"></i>İşlemler<i class="icon-chevron-down icon-white"></i>
                  </a>
                  <ul class="dropdown-menu">
                    <li><%= link_to "Add Contact", new_contact_path(:company_id => @company.id) %></li>
                    <li><%= link_to "Add Activity", edit_company_path(@company) %></li>
                    <li><%= link_to "Add Opportunity", edit_company_path(@company) %></li>
                    <li><%= link_to "Edit Company", edit_company_path(@company) %></li>
                  </ul>
                </div>
              </li>
            </ul>
            <div id="my-tab-content" class="tab-content">
              <%= render 'tab1', :position => @position %>
              <%= render 'tab2', :position => @position %>
              <%= render 'tab3', :position => @position %>
            </div>
          </div>
        </div>
      </td>
      <!--td id="rightCell" class="sidebarCell">
        <div id="rightbar">
          <%= render 'news', :company => @company %>
          <%= render 'numbers', :company => @company %>
          <%= render 'followers', :company => @company %>
        </div>
      </td-->
    </tr>
  </table>
</div>
