<div id="company_profile" class="accordion-body collapse">
  <div class="pageBody accordion-inner">
    <table class="detailList" width="100%" border="0" cellpadding="0" cellspacing="0">
      <tr>
        <td class="labelCol2"><%= t("defaults.label.address") %> :</td>
        <td class="dataCol2"><%= "#{@company.address} #{@company.postcode} #{@company.district}" %></td>
        <td class="labelCol2"><%= t("defaults.label.notes") %> :</td>
        <td class="dataCol2 lastCol"><%= @company.notes %></td>
      </tr>
    </table>
  </div>
</div>
