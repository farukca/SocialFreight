<div class="navbar navbar-fixed-top">
  <div class="navbar-inner fill">
    <div class="container-fluid">
      <%= link_to "SocialFreight", root_url, :class => "brand" %>
      <div class="nav-collapse">
        <div class="pull-right">
          <ul class="nav pull-right">
            <% if current_user %>
              <li><%= link_to "Others", "#", :class => 'togglebutton', "data-href" => "#socialCell" %></li>
              <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.to_s %> <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li><%= link_to dashboard_path do %><i class="icon-cog"></i>Preferences<% end %></li>
                  <li>
                    <% if current_user.person %>
                      <%= link_to person_path(current_user.person) do%><i class="icon-user"></i>Profil<% end %>
                    <% else %>
                      <%= link_to new_person_path do%><i class="icon-user"></i>Profil<% end %>
                    <% end %>
                  </li>
                  <li><%= link_to new_task_path do %><i class="icon-tasks"></i>Yeni Görev<% end %></li>
                  <li class="divider"></li>
                  <li><%= link_to logout_path do %><i class="icon-off"></i>Logout<% end %></li>
                </ul>
              </li>
            <% else %>
              <li><%#= link_to "Kayıt Ol", signup_path %> </li>
              <li><%= link_to "Giriş Yap", login_path %></li>
              <li><%= link_to "Ücretsiz Deneyin", new_patron_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>
