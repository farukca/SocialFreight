<div class="pageHeader">
  <table cellspacing="0" cellpadding="0" border="0">
    <tbody>
      <tr>
        <td class="pbTitle">
          <h3 class="mainTitle"><%= header.humanize if header %></h3>
        </td>
        <td id="topButtonRow" class="pbButton">
          <%= f.button :submit, "Kaydet", :class => 'btn primary' %>
          <%= f.button :submit, "Kaydet & Yeni", :class => "btn primary", :name => "saveandnew" %>
          <input class="btn danger" type="submit" name="cancel" title="Cancel" value="Cancel">
        </td>
      </tr>
    </tbody>
  </table>
</div>
