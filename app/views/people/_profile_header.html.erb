<div id="profile-header" class="profile-content">
  <table class="outer" width="100%" id="headerTable" border="0" cellspacing="0" cellpadding="0">
    <tr>
      <td class="span2 noborder">
        <div id="profile-picture" class="loading">
          <%= link_to image_tag(person.avatar_url(:profile)), "#", :class => 'togglebutton', "data-placement" => "below",  "rel" => 'twipsy', "title" => "Resmi Değiştirmek için Tıklayınız", "data-href" => "#logo_change_div" %>
        </div>
      </td>
      <td>
        <div id="profile-detail" class="loading">
          <div id="profile-title" class="row-fluid">
            <div class="title">
              <span><%= "#{person}" %></span>
            </div>
            <div class="buttons">
              <% if current_user.id = person.user_id %>
                <%= link_to "Edit", edit_person_path(person), :class => 'btn primary' %>
              <% else %>
                <%= render 'shared/follow_button', object: person.user %>
              <% end %>
            </div>
          </div>
          <%= render 'people/profile_detail', :person => person %>
        </div>
        <div id="logo_change_div" class="slidingDiv">
          <%= form_for person, :remote => true, :html => {:multipart => true} do |f| %>
            <p>
              <label>Resim dosyası:</label>
              <%= f.file_field :avatar %>
              <%= f.submit "Değiştir" %>
              <%if person.avatar? %>
                <%= f.check_box :remove_avatar %>&nbsp;Logoyu Sil
              <% end %>
            </p>
          <% end %>
        </div>
      </td>
    </tr>
  </table>
</div>
