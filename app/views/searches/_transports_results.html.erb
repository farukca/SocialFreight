<div id="report-results">
  <%= render 'searches/transports_form' %>
  <div class="page-header">
    <h4>Taşıma (Sefer) Listesi
    	<small><%= sanitize page_entries_info @transports %></small>
	  	<small class="pull-right">
	  		<a href="#" class="saveascsv" data-table="contact_report_results">Excele Aktar</a>
	  	</small>
    </h4>
  </div>

  <div class="row-fluid">
    <%= render 'transports/related_list', :objects => @transports %>
  </div>

  <div class="row-fluid">
    <%= paginate @transports %>
  </div>
</div>