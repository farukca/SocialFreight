<div id="report-results">
  <%= render 'searches/companies_form' %>
  <div class="page-header">
    <h4>Kişi Listesi
    	<small><%= sanitize page_entries_info @contacts %></small>
	  	<small class="pull-right">
	  		<a href="#" class="saveascsv" data-table="contact_report_results">Excele Aktar</a>
	  	</small>
    </h4>
  </div>

  <div class="row-fluid">
    <%= render 'contacts/related_list', :objects => @contacts %>
  </div>

  <div class="row-fluid">
    <%= paginate @contacts %>
  </div>
</div>