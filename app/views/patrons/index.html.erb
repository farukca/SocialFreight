<div class="row-fluid">
  <div class="page-header">
    <h4><%= t("patrons.title.patron_list") %></h4>
  </div>

  <div class="row-fluid">
    <table class="table table-condensed">
      <tr>
        <th><%= t("patrons.label.name") %></th>
        <th><%= t("defaults.label.tel") %></th>
        <th><%= t("defaults.label.email") %></th>
        <th><%= t("defaults.label.country") %></th>
        <th><%= t("defaults.label.city") %></th>
        <th></th>
        <th></th>
        <th></th>
      </tr>

      <%= render @patrons %>

    </table>
  </div>
</div>