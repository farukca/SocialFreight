<%= table_for objects, html: { class: "table table-condensed table-striped table-fixed" } do -%>
  <% column :name, html: { th: { class: "span2" } } %>
  <% column :surname, html: { th: { class: "span2" } } %>
  <% column :email, html: { th: { class: "span2" } } %>
  <% column html: { th: { class: "span2" } } do |user| %>
    <% "#{user.last_login_at.strftime("%d/%m/%Y %I:%M%p")}" if user.last_login_at %>
  <% end %>
  <% column html: { th: { class: "span2" } } do |user| %>
    <% "#{user.last_activity_at.strftime("%d/%m/%Y %I:%M%p")}" if user.last_activity_at %>
  <% end %>
  <% column html: { th: { class: "span1" } } do |user| %>
    <% link_to "Güncelle", edit_user_path(user) %>
  <% end %>
<% end %>